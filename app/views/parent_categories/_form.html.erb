<%= form_for @parent_category, :html => { :class => "form-horizontal parent_category" } do |f| %>

    <% if @parent_category.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@parent_category.errors.count, "error") %> prohibited this parent_category from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @parent_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@parent_category[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            parent_categories_path, :class => 'btn btn-default' %>

<% end %>
